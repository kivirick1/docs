---
title: "🧩 Editbox (drawEdit)"
---
```lua
drawEdit(
	id,
	x,
	y,
	w,
	h,
	text,
	icon,
	customize
)
```

### Parametreler
- `id` (string) — Editbox benzersiz kimliği.
- `x, y, w, h` (number) — Konum ve boyut.
- `text` (string) — Placeholder metni.
- `icon` (string, optional) — FontAwesome ikonu.
- `customize` (table, optional) — Özelleştirme tablosu.

### Dönüş Değeri
- (string) — Editbox’ın mevcut değeri. (Kullanıcı bir şey yazmadıysa `""` döner.)

### `customize` Anahtarları
| Anahtar | Tip | Varsayılan | Açıklama |
| --- | --- | --- | --- |
| `radius` | number | `5` | Köşe yuvarlama. |
| `alpha` | number | `1` | Opaklık (0–1). `disabled=true` iken otomatik 0.5 ile çarpılır. |
| `backgroundColor` | table/color | `{"secondary","dark"}` | Arka plan rengi. |
| `textColor` | table/color | `{"text","light"}` | Yazı rengi. |
| `titleColor` | table/color | `{"text","hover"}` | Placeholder rengi. |
| `iconColor` | table/color | `{"primary","dark"}` | İkon rengi. |
| `caretColor` | table/color | `{"primary","hover"}` | İmleç rengi. |
| `selectionColor` | table/color | `{"primary","dark"}` | Seçili alan arka plan rengi. |
| `textFont` | string | `"default"` | Yazı tipi adı (`getFont` ile). |
| `textSize` | number | `10` | Yazı tipi boyutu. |
| `iconSize` | number | `12` | İkon boyutu. |
| `disabled` | boolean | `false` | Devre dışı bırakır (hover/focus kapanır, yazma engellenir). |
| `password` | boolean | `false` | Şifre modu (metni `*` ile gösterir). |

### Çerçeve (line) Ayarları
| Anahtar | Tip | Varsayılan | Açıklama |
| --- | --- | --- | --- |
| `line` | boolean | `false` | Çerçeve çizilsin mi? |
| `lineOnHover` | boolean | `false` | Üzerine gelince çerçeve. |
| `lineOnFocus` | boolean | `false` | Odaklanınca çerçeve. |
| `lineType` | string | `"line"` | `"line"` veya `"dash"`. |
| `lineDash` | number | `10` | `lineType="dash"` iken dash değeri. |
| `lineColor` | table/color | `{"primary","dark"}` | Normal çerçeve rengi. |
| `lineHoverColor` | table/color | `{"primary","hover"}` | Hover çerçeve rengi. |
| `lineFocusColor` | table/color | `{"primary","hover"}` | Focus çerçeve rengi. |

### Örnek
```lua
drawEdit(
	"username",
	100, 100,
	250, 30,
	"Kullanıcı adı...",
	"",
	{
		radius = 6,
		alpha = 0.9,
		lineOnFocus = true,
		textColor = {"text","light"},
		caretColor = {"primary","hover"},
		selectionColor = {"primary","dark"},
		textFont = "default",
		textSize = 11
	}
)
```